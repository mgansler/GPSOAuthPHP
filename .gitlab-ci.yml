default:
  script:
    - echo 'extension="xdebug.so"' >> ~/.phpenv/versions/$(phpenv version-name)/etc/conf.d/ci-runner.ini
    - composer install
    - php vendor/phpunit/phpunit/phpunit --coverage-text